# ACTIVE_WORK.md

This file tracks the current working session: two tiers of checklist — **Overarching Goals** and the granular **Tasks** grouped under them — plus a **Debugging** log and long-lived reference notes. The overarching goals mirror the Project Goals in [DESIGN.md](DESIGN.md); keep them in sync. Completed fixes and tasks are logged in [CHANGELOG.md](CHANGELOG.md), not here.

---

## Overarching Goals

- [x] **0. External Flash (OSPI) Chip Detection** *(completed)* — Resolved hardware probing failure by mirroring working bootloader initialization; correctly identifies chip size.
- [x] **1. Headless, button-driven boot** *(completed)* — instant display-off boot; 1-2-3 priority (Buttons > Magic > Launcher).
- [x] **2. Dual-boot via bank swap** *(completed)* — Run the stock OFW (Bank 2) and Retro-Go (Bank 1) side-by-side via the `SWAP_BANK` option bytes. Once swapped into the OFW, operation is clean (§6).
- [x] **3. Recovery hook** *(completed)* — START at boot / LEFT+GAME in-game returns to the launcher.
- [x] **4. Themed menu with authentic game art** *(completed)* — Mario & Zelda art extracted from the real ROMs.
- [x] **5. On-screen diagnostics & system info** *(completed)* — battery, bank use, FPS, magic cells in a toggleable submenu.
- [x] **6. Read-only File Browser** *(completed)* — Browse filesystems (LittleFS/FrogFS/FAT) directly from the launcher.
- [x] **7. Power management** *(completed)* — Unified power button handling and 30s inactivity auto-hide mode.
- [x] **8. Flash size reduction** *(completed)* — Dieted RAM-boot + 128KB LZMA dictionary freed ~10KB total.
- [x] **9. Boot resilience hardening** *(completed)* — Protected Retro-Go warm resets via DTCM Safe Zone and direct stub jumps.
- [x] **10. Refactor and Modularize Codebase** *(completed)* — Clean architectural separation of engine, storage, and UI.
- [x] **11. Filesystem Integration & Dynamic Theming** *(completed)* — Enabled Read-Write LittleFS, full exFAT/LFN, and RTC timestamps.
- [x] **12. Codebase Bloat Cleanup** — Reduce technical debt and binary size by removing dead code and consolidating duplicates (see `docs/codebase-report.md`).
- [x] **13. SD-Card Support** — Browse and read/write FAT/exFAT SD cards from the launcher, built on an in-core SD block device (auto-detecting both hardware mods) and the PIE module loader (full FatFs RW+exFAT ships as a loadable module; a bare-bones RO FatFs is the in-core bootstrap). Detection runs off the boot-critical path, so the launcher stays reachable with or without a card. *(Functionally complete and verified on hardware; distribution polish — shrink-to-32K, the optional per-mod build, and the full docs rewrite — is tracked under the Phase 6 task list.)*
- [x] **14. Internationalized UI** *(complete, hardware-verified; SD install is a confirmed transient installer module, langsync removed)* — Mixed-case Fusion Pixel 12px monospaced font (Makefile `FONT_STYLE`) + UTF-8 renderer; every string externalized behind `tr()`; per-script `.fnt` fonts and per-language `.lang` packs on the device filesystem, selected live under Settings, with English/ASCII as the always-present fallback. Ships 19 languages (en/de/fr/es/it/pt/nl/pl/ro/ru/uk/el/ar/fa/ja/zh-Hans/zh-Hant/ko; Greek + Cyrillic share the `latin` font, Arabic + Farsi are RTL). A 2026-06 quality pass naturalized every pack in context (German verbal-noun progress, de-abbreviated footers, mixed-case yes/no) and routed the last hardcoded Partition Viewer English through `tr()`; fonts are now read **LFS-only** (no SD shadow), the CJK fonts are **complete** per script and **streamed glyph-by-glyph from LFS** so arbitrary CJK filenames render, a `{TOKEN}` replacer renders controller/logo glyphs baked into `latin.fnt`, and the installer deletes each SD source after installing it to LittleFS. (DESIGN §12, [docs/i18n.md](docs/i18n.md).)
- [x] **15. Retro-Go return-to-menu reliability** *(fixed + guarded; hardware-verified 2026-06-01)* — Retro-Go "Return to Main Menu" must ALWAYS **re-launch Retro-Go** so its own launcher reloads (the `"CORE"` / `BOOT_MAGIC_RESET` signals in the RG magic cell `0x20000000` → jump `RETROGO_BASE`), never fall through to the chainloader menu. Root cause of this (5th+) regression: the **stub** jumped to a hardcoded `0x08008000` that went stale when the ceiling rose 32K→40K (`RETROGO_BASE`→`0x0800A000`) — fixed to `RETROGO_BASE` (the define); also corrected `main.c` §2.1 so `CORE` re-launches. Permanently inviolable: it is a **LAW** in GEMINI.md (under "STABILITY IS LAW") that any change to the stub/boot magic handling must re-verify the in-game round trip on hardware before being called done. Standing regression test: `scripts/tests/retrogo_return_test.py`.
- [ ] **16. Module-facing GUI + localization** — give PIE modules first-class UI access so the launcher can grow features as modules without forking the look. *Started:* the **`gui_api_t` GUI-services vtable** (`system/gui_api.h`) exposes the core's RTL-aware draw primitives + modals to modules (Goal 14 / Phase B). The **feature-module framework** (`system/feature.{h,c}`, `docs/module-menu-registration.md`) lets a PIE module register its own Tools/Settings entry and/or handle a file type purely via its header manifest — the core discovers it at boot (`feature_discover`), splices it into the menu (Tools after Partition Viewer; Settings between Fast-Boot and Reset Defaults, Reset last), and transiently loads + runs it on selection, with **no core code naming any module** *(hardware-verified across themes + languages 2026-06-04)*. *Module i18n now realized (2026-06, hardware-verified):* a module localizes its UI **compiled-in, with no device-side files** — per-language JSON under `i18n/modules/<module>/` is cooked by `scripts/build/cook_modstrings.py` into an `[id][lang]` matrix baked into the module binary; the module picks its column from `gui->lang_code()` (the `gui_api_t` seam gained `tr()` + `lang_code()`) and resolves `ms(MS_*)`, English fallback. ar/fa are pre-shaped to visual order with token-safe footers (no full mirror), and the **menu entry itself** localizes via `MS_TITLE` packed into the module header (`menu_label_xlat`), resolved live by the core at discovery. First consumer: the MP3 player, fully localized in 18 languages (labels/buttons/help/footers). *Future:* (b) **modules register full pages** in the `ui` window stack / a richer widget toolkit layered on `gui_api`.
- [ ] **17. Clean file-operation merges:** pasting over an existing file or folder should merge cleanly into the destination, and on each name collision offer a choice to replace or skip the existing entry (with apply-to-all), instead of failing the paste or silently overwriting. Recursive folder paste merges directory-by-directory. Builds on the Goal 13 file-browser copy/paste.
- [ ] **18. Reusable file browser + MP3 player** — the in-core file browser is an instanceable, capability-gated engine (`fb_cfg_t`: pick-mode, extension filter, per-op gating); heavy file-ops moved to a transient `fileops` module so any feature module can reuse browsing/picking. First consumer: a simple **MP3 player** feature module (`/modules/features/mp3.bin`, `file_ext="mp3"`) launched by picking a `.mp3` in the browser; brings up SAI1+DMA at register level (PLL2 SAI clock + PE3 amp already up from `board.c`), streams + decodes via vendored minimp3, mono/48 kHz, entirely inside the module pool (memory-safe). See `docs/mp3-player-plan.md`, `docs/module-menu-registration.md`.

## Tasks

### QA Test Suite (branch `qa-test-suite`)
Environment-aware, SWD-observable-first hardware-in-the-loop suite. Full scope in [docs/qa-test-coverage-plan.md](docs/qa-test-coverage-plan.md).
- [x] **Enabling infrastructure** — `observe.py` (named SWD readers), `envprobe.py` (one `Environment` snapshot, validated live), `provision.py` (cheap toggles + `Recipe`/`apply`, `clean_reboot` shared with `test_abi_reject`), `scripts/tests/env/` (7 named fixtures + registry), `run_suite.py` (adaptive/matrix/tier modes + tactful report). *(host-verified; envprobe + provision toggles + clean_reboot verified on hardware)*
- [x] **OCR fixed + hardened (hardware-verified)** — `ocr.ensure_fonts()` regenerates the `make clean`-wiped fonts (proven end-to-end). Real device-frame bug found + fixed: rigid `locate()` is not discriminative on dense menus, so `detect_language` mislabeled a German screen as it_IT; the robust matcher is `Screen.contains()` (read rows + substring, `Screen(max_cp=0x600)` for speed). **CJK detection works** (ja/zh/ko detected exactly, `cjk_detect_test` 5/0). **RTL (ar/fa)**: cursive won't OCR + mirrored layout, so it's recovery-only via `provision.set_active_language()` rewriting `/i18n/.active` (`rtl_recover_test` 5/0). `capture_corpus.py` (SWD-navigated) + the offline oracle replay real frames.
- [x] **L0/L1 host + build gates** — settings-word, boot-magic table, lang/fnt parse, size ceiling, determinism (all green); build matrix authored.
- [x] **SWD device tests (hardware-verified)** — invariant menu-reachable (9/0), sd_detect (3/0), theme_persist (5/0), module_presence (6/0), idle_hide (6/0); `harness.go_home` (back out to main via `g_stack_ptr`) fixed `test_remote_input` (6/0).
- [x] **Bank-swap / OFW + destructive (hardware-verified, now headless)** — the OFW power-off-on-switch quirk is fixed (official gnwmanager), so these run autonomously: `retrogo_return` (CORE/RESET re-launch Retro-Go, the law), `boot_selector` (RETRO-GO + Mario + Zelda flash-from-backup → bank-swap → boot → Left+Game escape, 6/0). Full provisioning wired to the real Make targets (`flash_all`/`flash_rg`/`push_modules`/`push_i18n`); `provision.wipe_lfs_content()` (discover+remove) + `lfs_wipe_restore_test` prove the invariant holds with LittleFS wiped and restore from pre-built artifacts works.
- [ ] **Remaining breadth** — partition viewer + secure-erase, file-browser RW within existing dirs (no SD mkdir), full-extflash erase/restore (heavy), crash-log + boot-bench hooks, fast-boot RUN, the L4 matrix sweep. (Module/MP3 feature tests deprioritized per direction.)
- [ ] **Notes** — language persistence is `/i18n/.active` (LittleFS), NOT the BKP3R `SETTINGS_LANG` copy. SD cannot mkdir (gnwmanager limit): tests use pre-existing dirs / root files only.

### Picture Viewer + module-memory (branch `picture_viewer`)
- [x] **Picture Viewer feature module** (Goal 18's 2nd consumer): JPEG via the STM32 hardware JPEG codec (software tjpgd fallback), PNG via streaming inflate, sibling-image gallery, aspect-fit / fullscreen / zoom + pan, cancelable loading dialog, localized in all 18 languages. *(hardware-verified)*
- [x] **Module pool 192 KB → 384 KB** (`loader.c` `MODULE_POOL_BASE` `0x240C0000` → `0x24090000`, reclaiming the free AXI gap below it) so the 104 KB picture module loads past the resident modules — fixes "Out of module memory". *(hardware-verified: picture / mp3 / language all load cleanly; boot intact, uwTick live)*
- [x] Picture Viewer: default the starting directory to `sd:/pictures/` when it exists (falls back to the picker otherwise). *(hardware-verified)* — folder later retargeted to `sd:/screenshots/` (see BMP item below).
- [x] Picture Viewer: BMP support + default folder `sd:/pictures/` to `sd:/screenshots/`. New streaming `bmp.c`/`bmp.h` covering BI_RGB 1/4/8/24/32 bpp, BI_BITFIELDS 16/32 bpp (Retro-Go writes 16bpp RGB565 BITFIELDS for its screenshots), 16bpp BI_RGB (XRGB1555), bottom-up + top-down; reuses the shared row-callback resampler. Manifest now claims `jpg,jpeg,png,bmp`, which overflowed `module_header_t.file_ext[16]`, widened to `[24]` with a module **ABI bump 3 to 4** (all modules rebuilt). Host-verified bit-for-bit (`scripts/debug/bmp_verify.py`, 9/9, incl. a real Retro-Go screenshot). *(hardware-verified: Retro-Go screenshot renders in the viewer)*
- [ ] **Tiered module-memory architecture** *(branch `memory-architecture`)*: scale past "Out of module memory" by tiering RAM and OSPI. Prereq for a video player. Design in the plan file and `docs/memory-architecture.md`.
  - [x] **Stage 1: module pool 192 → 384 KB** (shipped to main, `0301f99`).
  - [x] **Stage 2: D2 shared scratch** (hardware-verified). `scratch_get()` borrows idle D2-2 SRAM (64 KB) for big transient buffers; picture's PNG window left the pool (104 → 38 KB footprint).
  - [x] **Stage 3a: extflash reslice + FAT module store** (hardware-verified: boots clean, mounts as `EXT-FAT-01`; OFW boot + Retro-Go round trips re-verified after the reslice via boot_selector 4/4 and retrogo_return). Contiguous SD-variant layout (backups behind the assets, FAT store at `0x540000`, LFS at `0xA00000`), an optional FAT partition holding the `/fs/` RW drivers, in-core RO FatFs reading it (BPB-sized), `make prepare-fat-bin` (pyfatfs). Fixed the `host_flash_read` EXTFLASH_BASE double-add the store exposed.
  - [x] **Stage 3b: two-base relocation + ABI 3** (hardware-verified, behavior-preserving). The loader relocates against two bases (a target below `data_offset` resolves vs the text base, at/above vs the data base); both contiguous = old single-base behavior. Necessary groundwork for XIP but NOT sufficient alone (see 3c).
  - [x] **Stage 3c: install + load (FAT XIP)** *(DONE + hardware-verified: XIP works via r9-pic, install generalized, LFN store enum; see [docs/memory-architecture.md](docs/memory-architecture.md) "As built")*. History: load plumbing was first built with XIP gated OFF: `fat_ro_map` (contiguity-checked flash-address map), `vfs_map_module`, `mod_load_image_xip`, the `g_mod_xip_loads`/`g_mod_ram_loads` counters, `make_fat_image.py` module inject, and `fat_contig.py`, all behind `g_xip_enabled=false`. On-hardware test found the real blocker: `-fPIC` modules bus-fault when XIP'd (PC-relative GOT vs non-contiguous .text/.got). XIP needs modules built `-msingle-pic-base` (GOT base in r9), the loader setting r9 before entry, and an ABI bump (see the [[xip-needs-single-pic-base]] memory). Scope XIP to TRANSIENT modules (mp3/picture: one r9-set at entry, the big wins); keep resident modules full-copy.
    - [x] **Must-have: file browser `f_expand` write to EXT-FAT** *(done)* so manually-moved modules land contiguous (XIP-able). Memory-mapped FAT store ONLY (not the SD: a block device, never XIP'd, where `f_expand` can fail for no benefit). Needs the loaded FAT-RW driver to gain an `f_expand` create (FF_USE_EXPAND=1), `vfs_driver_t` an `open_contiguous` callback, and the copy path to use it for an EXT-FAT destination.
  - [ ] **Stage 3d: power-safe FAT compactor + Stage-3 loose ends** — the one open item. v1 graceful degradation is in place (f_expand best-effort falls to the loader's full-copy, so XIP just does not engage). The transactional defragmenter (commit-last, dirty-flag boot scan, X% reserve, battery gate) plus the loose ends (full LFN map, Latin-RAM enforcement, store provisioning cleanup, fonts-XIP reclaim) are in [docs/fat-compaction.md](docs/fat-compaction.md). STABILITY IS LAW: trace every failure mode (power loss mid-move) before writing it.
  - [ ] **Fonts XIP (~36 KB back)** — partially done (the whole-load buffers shed ~16 KB back to the pool); the rest (XIP CJK glyph data, drive the resident language module ~48 → ~12 KB) is tracked in [docs/fat-compaction.md](docs/fat-compaction.md).
- [x] **Validate OFW asset data before booting an OFW** *(branch `ofw-crc-verify`, hardware-verified)* — CRC32 (HW CRC unit) the OFW backup image AND its external asset blob before the Bank-2 copy/swap, and fail loud instead of silently booting a half-flashed/corrupt OFW. The patcher bakes a CRC-32/MPEG-2 signature of each patched image + asset static window (save regions excluded) into committed `src/common/ofw_crc.h` (`make ofw-crc`); `system/ofw_verify.c` re-checks both with the HW CRC unit in `partition_flash_ofw` (now returns bool), and the menu + god-mode boot paths refuse a mismatch and stay in the launcher. Partition Viewer shows a mismatched OFW/asset as `UNKNOWN`. Verified: genuine OFWs boot (`boot_selector` 6/0); stock FW at the real offset is recognized but refused, Bank 2 untouched (`scripts/tests/ofw_crc_gate_test.py` 7/0). **Remaining (next session):** apply the same gate to Retro-Go; (optional) hardware-confirm the Partition Viewer `UNKNOWN` label visually.

### Goal 1 — Headless, button-driven boot
- [x] Implement Toggle Fast-Boot setting via backup register 3 with PAUSE/SET god-mode override. *(verified on hardware)*

### Goal 12 — Codebase Bloat Cleanup
- [x] **Safe Deletions** (Items 1, 5, 6, 8, 16, 17, 28)
    - [x] Delete `src/patch/mx_ospi_init.c` (Item 1).
    - [x] Remove `ui_draw_battery` and `ui_draw_simple_background` (Items 5, 6).
    - [x] Remove `asset_loader_init` (Item 8).
    - [x] Remove dead declarations: `gui_draw_theme_background`, `menu_flash_firmware`, `lfs_test_mount` (Items 16, 17, 28).
- [x] **Feature Toggles**
    - [x] Introduce build-time flags for `int_to_str_w` and its hidden dependencies (Item 9, 14).
- [x] **Asset Generation Optimization**
    - [x] Fix `cook_assets.py` to only bake referenced assets and remove hardcoded count (Item 21).
- [x] **Structural Consolidation**
    - [x] Merge `update_progress` and `update_flash_ui` in `partition.c` (Item 11).
    - [x] Parameterize bank-swap helpers in `banks.h` (Item 12).
- [x] **Refactoring & Cleanup**
    - [x] Fix hardcoded asset count and minor API inconsistencies (Items 15, 18, 23, 26).
- [x] **Phase 2: Boot & Structural Deduplication**
    - [x] Consolidate boot-magic logic and delete `stub_logic.c` (Items 2, 3).
    - [x] Consolidate shared interrupt handlers and spin delays (Items 4, 29).
    - [x] Optimize partition sorting in `partition.c` (Items 13, 14).
    - [x] Minor cleanups: `LZMA_HEAP_RESET` macro and GPIO init comments (Items 24, 27).
- [x] **Phase 3: GUI Font Optimization**
    - [x] Replaced 512-byte RAM `dynamic_font` with direct-indexed flash `unscii-8` subset (ASCII + 8 common diacritics).
    - [x] Added minimal UTF-8 decoder to `gui_draw_text` for combining diacritics support.
- [x] **Phase 4: Byte-focused DRY pass** *(verified on hardware via framebuffer capture)*
    - [x] Collapsed `gui_draw_text` into `gui_draw_text_marquee` (removed the duplicate UTF-8 decoder + draw loop).
    - [x] Added `gui_clip_rect` helper; merged `gui_draw_dim_rect`/`gui_draw_frost_rect` into `gui_draw_blend_rect(...,frost)`.
    - [x] Removed redundant extension-uppercasing in the file browser (`gui_draw_char_clipped` already uppercases).
    - [x] Table-drove GPIO setup (`MX_GPIO_Init`, `HAL_OSPI_MspInit`, `HAL_LTDC_MspInit`) via a shared `gpio_init_table`.
    - Result: app `.text` 39,672 → 39,128 B; flashed payload 27,928 → 27,562 B (−366 B). Note: with `-flto`+`--gc-sections`, unreferenced functions are already stripped, so removing dead code (intentionally skipped this pass) yields ~0 bytes — wins came from consolidating *linked* code.
- [x] **Phase 5: Low-Level Register & Boot Optimization** *(verified on hardware)*
    - [x] Truncate the stub's interrupt vector table in `startup.s` to exclude unused external interrupts (-620 B).
    - [x] Replace all HAL GPIO write/read calls with bare-metal `BSRR`/`IDR` register writes/reads across `gui.c` and `board.c`, allowing the compiler to strip `HAL_GPIO_Init`.
    - [x] Replace `HAL_PWR_EnableBkUpAccess()` with direct register writes to `PWR->CR1` to enable compiler stripping of this HAL routine.
    - [x] Simplify `stub_lzma_alloc` by removing runtime reset logic and bounds checking.
    - [x] Optimize `board_is_valid_app` using high-byte checks before checking sub-boundaries to minimize instruction counts.
    - Result: final binary size 32,752 → 31,984 B; free space 16 → 784 B (+768 B).
- [x] **Phase 6: Retro-Go SD Card Makefile Targets**
    - [x] Renamed `build_retro_go`/`flash_retro_go` to `build_rg`/`flash_rg`.
    - [x] Implemented dynamic `RG_FILESYSTEM_SIZE` calculation based on `RG_SD_CARD` state.
    - [x] Updated `flash_rg` to conditionally skip `frogfs.bin` and shift `littlefs.bin` to 8MB when building for SD card variants.
    - [x] Added `build_rg_sd` and `flash_rg_sd` targets.
### Goal 4 — Themed menu with authentic game art
- [x] Implement Universal Asset System: 'Cooker' pipeline, LZMA Super-Blob, and unified `gui_draw_asset` API.
- [x] Fix Mario font glyph corruption via palette buffer expansion (128 entries) and cache alignment.
- [x] Fix jumbled multi-tile sprites via grid-aware horizontal flipping.
- [x] Implement authentic Zelda II Link walking sprites extracted from the ROM.
- [x] **Usage-Based Dynamic Asset Cooking**
    - [x] **Pipeline Intelligence**: Refactor `cook_assets.py` to recursively scan `src/chainloader/` for `ASSET_` symbols.
    - [x] **Dynamic Filtering**: Only bake assets into `assets_gen.c` if they are actually referenced in the source code.
    - [x] **Font Standardization**: Abandon OFW font extraction; standardize on the clean 8x8 `fallback_font` for all themes.
    - [x] **Space Optimization**: Drastically reduced binary bloat; Mario assets now use compact tile indices.
- [x] Build richer / less-janky Zelda sprite scenes. (Gilded Forest theme + Moss Green).
- [x] Fix the weird Triforce in the top-center of the Zelda menu. (Removed crude rectangles).
- [x] Remove "active OFW" text from themed menus while preserving layout spacing.
- [x] Consolidate debug scripts (Mario+Zelda+NES) onto the shared `scripts/common/` library + 7 parameterized tools.
- [x] Flash and verify the visual menu output on-screen.

### Goal 7 — Flash size reduction
- [x] Implement RAM-boot architecture: Flash stub decompresses main launcher into AXI-SRAM.
- [x] **Optimized Space-Saving Architecture**
    - [x] Moved boot hierarchy (Button God-Mode, SRAM Magic) into the compressed app.
    - [x] Minimized Flash Stub: Bare-metal clock/power setup, POR standby check, and immediate LZMA inflation.
    - [x] Maximized LZMA Dictionary: 128 KiB dictionary provides massive compression for the exFAT-enabled app.
- [x] Drop heavy standard library usage: `sprintf` replaced by lean custom formatting.
- [x] Aggressive LTO and optimization flags tuning.
- [x] **LZMA-stream tuning (2026-06-04): ARMTHUMB BCJ pre-filter + lc1/lp1/pb1.** The biggest lever turned out to be the compression stage, not the source: prefiltering the Thumb-2 app with the BCJ filter + tuned params, with the stub reversing both, took Free space 76 -> 1780 B. See [docs/size-optimization.md](docs/size-optimization.md).
- [x] **Modularize .lang discovery/load into the language module (2026-06-04).** Removed duplicated `.lang` format code from core `vfs.c`; the module now uses generic `vfs_lfs_enum_dir` + `vfs_lfs_read`. 1780 -> 1948 B free. Hardware-verified (German menu).
- [ ] **Untangle + move OFW asset extraction into the theme module (FUTURE, agreed).** `board_load_dynamic_assets` (`assets.c`) belongs in `/modules/theme` but is entangled with the boot sequence, the base GUI-color setup, and fragile OFW-specific magic offsets + deps (ospi/valid-app/scratch/lzma/ui_update_theme). Do it as its own deliberate task: split base color setup (stays) from sprite tileset/palette extraction (moves), add the few host hooks, delete the already-gc'd dead core blitter source (`gui_draw_asset*`), and verify by switching to the sprite theme on hardware. ~1 KB; deferred because current headroom (1948 B) does not justify rushing a fragile change.

### Goal 11 — Filesystem Integration & Dynamic Theming
- [x] **LittleFS write support**
    - [x] Remove `LFS_READONLY` from `lfs_config.h` (via Makefile CFLAGS).
    - [x] Implement `lfs_flash_prog` and `lfs_flash_erase` in `lfs_wrapper.c` using OSPI APIs (with mode switching).
    - [x] Widen cache/page buffers to 256 bytes.
    - [x] Expose `lfs_mount_at(addr, block_count)` for dynamic mount from the partition scanner.
- [x] **Lightweight FrogFS reader**
    - [x] Create `src/chainloader/storage/frogfs_reader.h` and `frogfs_reader.c`.
    - [x] Implement `frogfs_open_dir`, `frogfs_read_dir`, `frogfs_close_dir` using direct header parsing (no decompression, no malloc).
- [x] **FatFS with exFAT (Dynamic Driver Loading)**
    - [x] Exclude FatFs from main launcher to fit within 32,767 bytes limits (Final: 30,780 bytes).
    - [x] Create Virtual File System (VFS) abstraction (`vfs.c`/`vfs.h`) with on-demand RAM loading.
    - [x] Create dynamic `fatfs.bin` compiled to target SRAM origin `0x240A0000`.
    - [x] Implement dynamic driver load from `/modules/filesystems/fatfs.bin` on SD card access.
    - [x] Deploy chainloader and driver binaries to device.
    - [x] Verify dynamic filesystem mounting and read-write/delete operations on-device (waiting for SD card mod hardware installation; dynamic driver loading, mounting, and deployment automated via Makefile).
- [x] **Dynamic LittleFS RW Driver**
    - [x] Extract LittleFS RW logic into dynamic driver `lfs_rw.bin` compiled to target SRAM origin `0x240C0000`.
    - [x] Implement dynamic loading of `lfs_rw.bin` in AXI SRAM when mounting a LittleFS partition.
    - [x] Update `vfs_get_driver` to search registered drivers in reverse order, allowing dynamic driver override.
    - [x] Implement the `install` Makefile target for both `fatfs.bin` and `lfs_rw.bin`.
    - [x] Polish File Browser / VFS copy-paste, error modals, Partition Viewer, and extract Zelda tileset.

### Goal 13 — SD-Card Support
- [x] **In-core SD block device** (`storage/sdcard.{c,h}`), ported from the in-repo bootloader reference: auto-detects SPI1/"Tim" (dedicated pins) and SoftSPI/"Yota9" (OSPI-tap); runs off the boot-critical path; always restores OSPI memory-mapped mode. *(verified: reads + writes a real SDHC card)*
- [x] **FAT-RW+exFAT as a PIE module** (`fatfs.bin`, GMOD format): `host_api` `sd_read`/`sd_write`; the module diskio routes to the card when the volume base is the SD sentinel. The in-core FatFs stays a bare-bones RO bootstrap (LFN off, CP437). *(verified)*
- [x] **Module versioning** — `module_header_t.version`; the loader scans every read-capable filesystem (SD first) and loads the highest-version copy of a module (`vfs_read_module`). *(field + selection logic in; the multi-version preference itself not yet exercised on hardware)*
- [x] **File browser** — packed name pool (full names, ≤512 files/dir, alphabetized); recursive folder copy + recursive folder delete; whole-tree + single-file pre-flight space checks; self-copy guard; cooperative cancel/progress on long ops; SD re-detect on entry; `LITTLEFS RW`/`FAT RW` labels; slim solid scrollbar; `../` parent entry. *(verified; recursive delete flashed and pending a final on-device confirmation)*
- **Phase 6 (remaining):** 40 KiB is the accepted chainloader ceiling (`RETROGO_BASE = 0x0800A000`, Retro-Go = 216 KiB); reclaiming back under 32K via further modularization is a nice-to-have, not a release gate. `docs/sdcard-integration-plan.md` marked historical and `docs/pie-module-loader.md` status refreshed. (optional) per-mod distribution build still open. *(Done: `pie-module-loader` + `fast-module-probe` squashed onto `main` as two commits, superseding the old `sdcard_implementation` branch.)*
- [ ] **Idea — compress the FS driver modules with LZMA.** The LZMA decompressor is already linked into the build, so storing `fatfs.bin`/`lfs_rw.bin` compressed on the LittleFS would shrink their on-device footprint (and speed the push). The loader would inflate the image into its bump-allocated slot before applying R_ARM_RELATIVE relocations. Weigh against the added loader complexity and RAM for the inflate.
- [ ] **Module dependencies — let a module declare what it needs; the loader resolves it.** A module's header (or a small manifest) lists its dependencies: another module and/or a filesystem class (e.g. "needs the LFS-RW module + an LFS partition"). The loader ensures those are present/loaded before the module, else declines it gracefully (the menu stays reachable). Motivating case: a **custom theme** module needs LFS-RW + an LFS partition because it reads/writes a small LFS file naming the chosen theme. Ties into the planned **theme-selector redesign** — Fallback / Default / Fancy (with an asset selector) / **Custom**, where Custom persists its choice (which the core knows nothing about) to LFS and the custom theme module renders it. Generalizes the ABI gate: ABI says "is this artifact built for this firmware?", dependencies say "is its environment satisfied?".
- [ ] **Idea: a GUI-services PIE module (`theme_driver_t` rendering without the asset pipeline).** Expose aspects of the core GUI to a PIE module over the host vtable (a `theme_driver_t`'s colors + `draw_*` callbacks, text, primitives) WITHOUT pulling in the expensive asset-cooking / sprite-blitter logic, so a module can drive the look of the UI cheaply and only pay for the sprite path if it actually wants it. Lets a custom or third-party theme render through the core's GUI primitives. Ties into the theme-selector redesign + the module-dependencies idea above. *(future session, not now.)*
- [x] **Remote input over the probe (autonomous UI testing).** `make REMOTE_INPUT=1` compiles a hook (chainloader `system/input.c` + OFW patch `read_buttons`) that OR's a debug-probe-written bitmask at `0x2001FFF4` into the live button state. Host side: shared backend `scripts/common/remote_input.py` (persistent OpenOCD connection, `button_press()` tap/hold/repeat API, `InputTransport` seam for a future Pi-GPIO rig) + `scripts/common/harness.py` (symbol resolve, menu-selection read, closed-loop `navigate_to`, `wait_u32` across resets); manual front-end `scripts/debug/remote_control.py` (keyboard + evdev gamepad); tests in `scripts/tests/` (depend only on `common/`). Now compiled in by default (opt out with `REMOTE_INPUT=0`); ~8 B device-side. Architecture in DESIGN.md §10.1. *(verified end-to-end on hardware: single-tap nav by exactly one row, and switch-to-Mario → boot OFW → Left+Game escape back to the launcher — the post-swap power-on is a known hardware quirk the test prompts for)*

### Goal 14 — Internationalized UI
- [x] **Phase 1 — Renderer + string externalization** *(verified on hardware)* — In-core Fusion Pixel ASCII font (style via Makefile `FONT_STYLE`, default 12px-monospaced) (`cook_font.py ascii` → `ui/gui_font.c`, ~1 KB); UTF-8 decode + proportional widths in a HAL-free `gui_text.c` (`gui_utf8_next`/`gui_glyph`/`gui_text_width`), all `strlen*8` width sites converted; `string_id_t` enum + `tr()` (`ui/strings.{c,h}`), ~73 strings routed across menu / settings / file browser / partition viewer / flash-progress / errors. Host unit test (`make test_host`).
- [x] **Phase 2 — Shared script fonts on the filesystem** *(verified on hardware)* — `cook_font.py blob` → `<script>.fnt` (sorted codepoint index + packed 1bpp bitmaps); `ui/font_ext.c` reads it whole into RAM and binary-searches, baseline self-aligned via `ref_top`; `vfs_read_file()` (magic-less) reads arbitrary files SD-first. `.fnt` reader covered by the host test against the real `latin.fnt`.
- [x] **Phase 3 — Language packs + selector + translations** *(verified on hardware)* — `cook_lang.py` builds `.lang` packs (ABI-checked, English fallback per id) + per-script `.chars` (CJK font subsets) + the device registry (`ui/i18n_langs.h`); `ui/i18n.c` loads the active pack + script font, reverts to English on any failure; `SETTINGS` language byte (`boot_magic.h`) + live `LANGUAGE` value-selector (`menu.c`) that persists; boot loads the persisted language. Translations: de/fr/es/it/pt/nl + ja/zh-Hans/ko. `make i18n` / `make push_i18n` cook + deploy.
- [x] **Hardware verification** — live switch to German (accented Latin `ä ö ü ß`) and Japanese / Chinese / Korean (CJK) all render from external fonts incl. endonyms; choice persists across reboot; graceful English/ASCII fallback by design + unit tests. Screen tours: `scripts/tests/i18n_screens.py`, `scripts/tests/i18n_switch.py`.
- [x] **16 languages** — added Russian/Ukrainian/Greek (Cyrillic+Greek share `latin.fnt`), Polish, Traditional Chinese (`zh_hant.fnt`), and Romanian (Latin-Extended-B for ș/ț). Selector sorted by English name (English first), decoupled from the stored index. Unified pack naming: canonical `/i18n/<code>.lang`, with `/i18n/<code>.lng` as the 8.3 SD alias (langsync installs it as `.lang`). File browser shows `FAT-01:/` / `LFS-01:/`. All hardware-verified.
- [x] **Module Services + langsync** *(verified on hardware)* — host UI service (`system/mod_ui.h`) lets PIE modules show confirm/error modals; the `langsync` module (`src/modules/langsync/`, 188 B) persists a newer/SD-only language pack into LittleFS on user confirm (detect via `vfs_lang_versions`, write via `vfs_lfs_write_file`). End-to-end proven: a Polish pack on the SD only → reboot → Polish confirm prompt → install to LittleFS → idempotent. Follow-up: generalize SD install to `/i18n/*.lang` (FAT-RW for SD LFN). (DESIGN §12 / docs/i18n.md.)
- [x] **Redesign: self-describing packs + English-only baked + magic SD install** *(hardware-verified except the SD-install path)* — Pack format `'LNG2'` (76-B header: code+endonym+script); **only English baked**, all others **discovered** from `/i18n/<code>.lang` on LittleFS; selector sorted by code; runtime reads LittleFS-only (no per-switch SD scan); persist **by code** (`/i18n/.active`). SD install is **in-core, silent, scan-all** (`i18n_install_from_sd`) — drop `.lang` files (no `.lng`); "Updated N languages" notice; `build/i18n/` copies 1:1 to the SD. Supersedes the baked registry, the `.lng` alias, and the langsync module. Verified: discovery (en_US/de_DE/zh_CN), sort, Latin + CJK render, persist across reboot, Retro-Go round trip. (`78cabce`, `b5f4cf7`)
  - [x] **Device-verify the SD→LittleFS install path** — implemented + builds, but boot-time `i18n_install_from_sd` scan/copy wasn't verifiable from the probe (couldn't stage `SD:/i18n` — gnwmanager can't mkdir on the SD); confirm via the hands-on SD copy of `build/i18n/`.
  - [ ] **Skip the SD install scan on a warm Retro-Go return** — bounded + only writes when newer, but it runs every boot when `/i18n` is on the SD; could be skipped on a warm reset for snappier quit-to-menu.
  - [x] **Remove the now-inert langsync module** *(done)* — `src/modules/langsync/`, the loader hook `mod_load_langsync`, and its Makefile rule are gone (no references remain in `src/` or the build). The `mod_ui` host-to-module UI seam is intentionally **kept** (a deliberate module-to-UI API, not specific to langsync); its header comment already names the language module as the consumer.
- [x] **i18n expansion (branch `i18n-en-ar-rtl`), Phases A through D complete, hardware-verified.** See [docs/i18n-expansion-plan.md](docs/i18n-expansion-plan.md).
  - [x] **Phase A — English packs + string sweep + ABI 3→4** *(hardware-verified)* — Real `en_US` (mixed-case American) and `en_UK` (British register) packs that hide the all-caps baked English from the live selector (in-core sentinel renamed to reserved code `en`; fresh device defaults en_US→en_UK→sentinel). Hardcoded-string sweep (file browser, partition dividers, theme names, install notice, OFW titles) routed through `tr()` with new `str_fmt1_int`/`str_fmt1_str` splicers + one `compose_value` point. `STRINGS_ABI_VERSION` 3→4. Language row moved **first** in Settings; selector shows **`endonym (code)`** (also disambiguates en_US/en_UK and gives OCR an ASCII handle). All 17 packs back-filled to 91/91 (machine drafts pending native review; the new "one string, every language" rule + `scripts/build/i18n_backfill.py`). Language switching kept **live-on-scroll** (an A-press-commit variant was tried + reverted — it left non-Latin endonyms unrendered until commit). Host tests green; en_US round-trip + ABI-4 fixtures added.
  - [x] **Phase B — RTL mirror + module GUI-services API** *(mirror hardware-verified with the force-RTL build)* — One `gui_rtl` flag (set from `i18n_is_rtl()` on a language change, or forced by `make RTL_TEST=1`); two HAL-free helpers in `gui_text.c` (`gui_mirror_x` box-aware mirror + `gui_draw_text_aligned` right-anchor) carry the whole mirror. Shared renderers route through them: `ui_list_draw` (cursor right as `<`, scrollbar left, split divider/panes mirrored), `ui_draw_header` (title right / battery left), modals, `compose_value` (RTL framing), and the two split-pane detail columns. LTR is identity (byte-for-byte unchanged); host test covers the mirror math. Folded-in **`gui_api_t`** GUI-services vtable (`system/gui_api.h`): the core's RTL-aware draw primitives + modals exposed to PIE modules (handed to the theme module via `theme_host_api_t.gui`, appended backward-compatibly — no `MODULE_ABI` bump). `< >`/cursor glyph convention to be finalised with real Arabic. *(remaining: normal-build LTR + Retro-Go re-confirm, device geometry test, DESIGN write-up.)*
  - [x] **Phase C: Arabic + Farsi + full translations** *(hardware-verified).* `ar` + `fa_IR` packs render RTL via the Phase B mirror; Vazirmatn cooked offline (`arabic_reshaper` presentation forms + `python-bidi` visual order, glyphs raised 6 px), no runtime shaping. All 19 packs 92/92 (full-word `Read/Write` + `Read-only`, the in-core ASCII fallback kept terse `RW`/`RO`; firmware-form OFW suffix; per-locale `STR_DECIMAL_SEP`). `i18n_backfill.py` rewritten to a pure gap-filler (adds missing ids as the English placeholder, never overrides; translations live in the language files; per-file `_adopted` marks intentionally-English terms). GAME on the Launch row flashes the selected OFW into Bank 2 to preview its theme without booting or swapping; value-selector arrows render outward (`< value >`) in RTL. (`013ead7`)
  - [x] **Phase D: on-the-fly non-Latin font loading** *(hardware-verified).* `font_ext` gained a third AUX slot that loads a script `.fnt` on demand by Unicode range (Hangul -> ko, Kana -> ja, Han -> ja/zh/ko tried in order, Arabic -> arabic), cached by path, so CJK renders under any active UI language (e.g. a CJK filename in the browser) and degrades to `?` when the font or glyph is absent. Bounded to the cooked subset; raw Arabic filenames stay unsupported (the font carries only pre-shaped presentation forms and there is no runtime shaping). (branch `i18n-en-ar-rtl`)
  - [ ] **Translation-quality + untranslated-leak pass (branch `i18n-quality-pass`); built + cooked, pending hardware verification.** Fixes the nuances that gave the machine-draft translations away. German register corrected: the imperative progress form (`Kopiere Datei...`, `Lösche...`, `Schreibe...`) became informative verbal nouns with no trailing `...` (`Kopiervorgang`, `Löschvorgang`, `Schreibvorgang`, `Berechnung`), plus de-abbreviated footers and mixed-case `Ja/Nein`. Untranslated English in the Partition Viewer + scanner routed through `tr()`: new ids for the LittleFS `Blocks: %d`, FrogFS `Entries: %d`, the FREE/FAT/asset/backup/app-image details, the `Firmware` type, and the `Modules`/`Internal Flash`/`External Flash` scan banners; `SD CARD` reuses `STR_DIV_SDCARD`; the file-picker footer and SD-address placeholder routed too. Details now translate at **draw time** (`partition_info_t` carries a `string_id_t` + count formatted via `str_fmt1_int`), so they follow the live language, not the boot-time one (the scan runs before the language module loads). English restructured for translatability (decision: yes): `STR_PREPARING`/`STR_WRITING`/`STR_MOUNT_FAIL` + the two count labels are now `%s`/`%d` format strings, so each language places the value in natural order and the RTL-breaking trailing glue is gone. All 19 packs naturalized to 106/106 (`i18n_backfill.py --check` clean), cooked OK (fonts re-subset, Arabic/Farsi reshaped); core build green at 2008 B free. No `STRINGS_ABI` bump (append-only; the pack `str_count` gate rejects stale packs). Non-de/en choices are best-effort with low-confidence items logged in [docs/i18n-translation-notes.md](docs/i18n-translation-notes.md) for native review. **Next:** flash core + `language.bin` + `make push_i18n`, then verify the German UI + the Retro-Go round trip on hardware. Then Goal 16(a) module i18n (below).

### Goal 18 — Reusable file browser + MP3 player
- [x] **Feature-module framework** *(hardware-verified across themes + languages 2026-06-04).* Modules register Tools/Settings entries + claim file extensions via their header manifest; core discovers `/modules/features/*` at boot and splices/launches them with no module-specific core code (`system/feature.{h,c}`, `menu.c` splice, `loader.c` `mod_load_feature`). Example modules `example.bin` (Tools) + `example_set.bin` (Settings) prove placement; `feature_menu_test.py` green in en/es.
- [x] **In-core browser is instanceable + capability-gated** (`fb_cfg_t`: pick-mode, ext filter, per-op gating; `browser_open`/`browser_open_picker`); heavy file-ops moved to the transient `fileops` module.
- [x] **MP3 player feature module** *(hardware-verified 2026-06-04; normal-speed SD `.mp3` playback).* `src/modules/features/mp3/` streams the picked file via the host file API (`feature_host_t.file_open/read/close`), decodes with vendored minimp3, downmixes mono, linear-resamples to 48 kHz, double-buffers a register-level SAI1+DMA sink (`audio_sai.c`). Memory-safe (bounded pool buffers). Fixed two SAI clock bugs (SAI1SEL=PLL2P, MCKEN) found by measuring DMA NDTR over SWD. See [docs/mp3-player-plan.md](docs/mp3-player-plan.md).
- [x] **Play queue + playback UI** *(hardware-verified 2026-06-04).* Sibling `*.mp3` in the launched file's directory form the queue (`feature_host_t.list_dir`); LEFT/RIGHT skip prev/next, a finished track auto-advances (wraps), B stops. UI shows filename, "track X/N", and the elapsed MM:SS timer. (Fixed a launch crash: the rewrite dropped `g_h = h`, so `refill()` deref'd a NULL host -> UNDEFINSTR; root-caused via crash_log + addr2line to `refill`.)
- [ ] **Remaining polish:** wire `feature_host_t.pick_file` (Tools-launch picker via a second browser instance) so "MP3 Player" with no file opens a chooser; confirm clean exit silence on decode error + mid-track abort; audio-quality pass (resampler, FIFO underrun headroom); verify next/prev with multiple files on the card.

### Testing & QA: comprehensive coverage *(deprioritized 2026-06-03; superseded by forthcoming plans)*
> Note (2026-06-03): the remaining language/test/OCR backlog (this section's P0/P1/P2 and the i18n OCR item in the Debugging Log) is on hold; new plans supersede its necessity. Kept here for reference, not as active work.
Granular plan: [docs/qa-test-coverage-plan.md](docs/qa-test-coverage-plan.md). Goal: a parametrized, environment-aware suite that proves the "menu reachable in every state" invariant across named device environments (BARE, RG, DOCS, NO-EXTFLASH, STALE-ABI, SIZES, CORRUPT, SD variants), with full feature-to-test traceability. Confirmed decisions: cold-boot god-mode stays a manual procedure (the boot path is not touched for testing); provisioning automates cheap state toggles (registers, magic cells, module/pack push, Retro-Go reset-vector toggle) and reserves full flashes for the golden setup; features lacking an observable get build-flag-gated hooks (zero release bytes).
- [ ] **P0 (enabling + invariant):** provisioning library (`scripts/common/provision.py`), environment probe (`scripts/common/envprobe.py`), suite runner (`scripts/tests/run_suite.py`), the ENV-BARE and ENV-DOCS invariant sweeps, repoint the language-detection tests off the moved `g_current`/`g_langs` symbols (now in the PIE language module, so OCR-only), L1 build/size/determinism gates, keep the Retro-Go round trip green.
- [ ] **P1 (core feature gaps):** file-browser operations + filesystem read-back, partition viewer, theme sprite-module load/persistence/fallback, fast-boot setting, recovery hook, OFW switch matrix, SD-install device-verify, ABI stale matrix, i18n all-16 completeness + persist.
- [ ] **P2 (breadth + edge):** EXTFLASH_SIZE / RG_SD_CARD variants, ENV-CORRUPT resilience, diagnostics assertions, crash log, boot-bench regression, module versioning, exFAT/LFN/RTC, non-Latin OCR hardening, ship-build (`REMOTE_INPUT=0`) smoke, hardware-mod second unit.

## Debugging Log
- **FatFs file browser: stale "Empty folder" when stepping back out of a subdirectory (cosmetic, not a data issue; deferred).** Entering a folder (e.g. a `testö` of UTF-8-named files on the SD card) then pressing back to the parent renders "Empty folder"; backing out to the FS list and re-opening the FatFS volume restores it. Ruled out: the browser path math (`/testö` truncates correctly to `/`), the flash sector cache (the SD path bypasses it via `g_host->sd_read`), a `g_fat_dirs[2]` slot leak (a remount, which fixes it, does not reset `g_fat_dirs_used`), and a missing multi-block stop (the SPI1 read does send CMD12 + skips the stuff byte). What fits all of it: `FF_FS_TINY=1` keeps a single shared FATFS sector window for the whole volume, and a remount (`f_mount` -> `disk_initialize`) re-inits it, so after the subdir excursion `readdir("/")` hits early end-of-directory until remount. Likely aggravated by the macOS metadata the card now carries at the root (`._testö`, `.Spotlight-V100/`, `.Trashes/`, `.fseventsd/`, a `._name` per file). Pre-existing + SD-specific; surfaced while testing Phase D. To pinpoint: instrument `scan_files` on-device to log `opendir`/`readdir` results for the failing re-scan, or test `FF_FS_TINY=0` (per-object buffers, more module RAM, confirm it still fits the pool after Phase D's +16 KB AUX). Deferred as out of scope.
- **i18n OCR non-Latin matching (#15) is not a font-metric problem (the obvious lead is a red herring).** *(Deprioritized 2026-06-03: superseded by forthcoming plans; #11 and #15 on hold.)* The OCR (`scripts/common/ocr.py`) loads the cooked `.fnt` glyphs and template-matches the screen; non-Latin (CJK / Greek / Cyrillic) does not match. The cooked CJK fonts differ from `latin.fnt` in cell metrics (`latin.fnt` cellH=12, ref_top=0; ja/zh/ko cellH=11, ref_top=1), which looked like the cause. But both the OCR (`ocr.py:86`) and the device font renderer (`src/modules/language/font_ext.c:71`) compute the per-glyph draw offset with the **same** formula `yoff = ref_top - GUI_FONT_REF_TOP` (`GUI_FONT_REF_TOP=1`), so the device and the template align identically. The metric difference is handled consistently on both sides and is NOT the cause. Greek and Cyrillic live in `latin.fnt` (same metrics as ASCII) yet also fail, confirming the cause is not metric-related. Diagnosis needs a device experiment: drive the device into a non-Latin language (chicken-and-egg, since the broken OCR cannot verify the language; cycle a known step count from English in code-sorted order, or read the active code another way), screenshot, then `locate()` the expected string and inspect the match score and which glyph the greedy `_best_glyph` matcher picks (Cyrillic 'Р' / Greek 'Α' look like Latin 'P' / 'A', a likely confusion). #11 (CJK launch-verb OCR) is blocked on this. Note: the language detector was repointed off the moved `g_current`/`g_langs` symbols to endonym-based OCR in `d9558c1`, so the Latin-language OCR tests work; only non-Latin remains.
- **ABI gating + installer-as-a-module — gates + transient installer module landed + verified.** Bumping `STRINGS_ABI_VERSION` (a boot-selector string change) and flashing only the core left the device running an ABI-3 core against the **stale ABI-2 language module + packs** on LittleFS → garbled non-English text. Principle: ABI is one compatibility contract gating **both load and install**, for **both** PIE modules (`MODULE_ABI_VERSION`) and `.lang` packs (`STRINGS_ABI_VERSION`); reject any mismatch in either direction and fall back to safe defaults; gating lives **in-core** (recovery-critical, must work with no module present). **Landed + hardware-verified (`81954a0`, `33fb308`):** in-core load gates (`loader.c` `mod_load_image`, `vfs_read_module`); the strings-ABI gate made **core-authoritative** (`vfs.c` enforces the core's own `STRINGS_ABI_VERSION`, not the `want_abi` a module passes — a stale module + stale packs used to "agree" and load); `module_header_t.abi` + a load-model `flags` field (resident vs `MOD_FLAG_TRANSIENT`); `mod_pool_mark`/`mod_pool_reset` (safe slot-reclaim, replacing the unsafe blanket `mod_reset`); and a shared `src/common/abi.h` (`module_abi_ok`/`pack_abi_ok`) the firmware **and** a host unit test (`scripts/build/test_abi_gate.c`, `make test_host`) both call — rejects abi±1 + bad magic for both header types (8/8). Verified on device: stale packs → clean English fallback (no garbage), then re-push restored languages. Build 37660 B (3300 free). **Stage 2 landed + hardware-verified (`e8a01b0`, `9d8ac83`, `f4775b8`, `bf22b82`):** moved SD→LittleFS install out of the language module into its own **transient** installer module (`src/modules/installer/`, `MOD_FLAG_TRANSIENT`: core loads it on demand, scan → per-class confirm → commit → `mod_pool_reset` reclaims the slot) handling **both** languages and modules with the same install gate; delete `install_one`/`i18n_install_arm`/`i18n_install_pump` from `lang_mgr.c` + `lang_api_t` + the `i18n.c` shim (supersedes the in-core `i18n_install_from_sd` in Goal 14). Both classes share one generic `artifact_kind_t` descriptor and the copy streams through the shared in-core `vfs_copy_open_file` (a 4 KiB buffer, reused by the file browser), so the installer holds no transfer buffer of its own (~17 KB to ~1.7 KB); the language module now only `rediscover()`s. Verified on hardware: `installer_test --kind all` 6/6 (a language pack and a module each offered with class-aware wording and re-installed from the physical SD), `theme_lang_test` + `retrogo_return_test` green. **On-device rejection tests now PASS on hardware, 6/6 (`6fa41e0`, `73977ba`).** A gated `ABI_SELFTEST` hook (`menu.c`, via `make ABI_SELFTEST=1`, zero bytes in a release build) runs the real gates and leaves two SWD-readable results: `vfs_read_module("/modules/_selftest.bin")` for the module gate and `vfs_lfs_lang_version("/i18n/_selftest.lang")` for the pack gate. A minimal dummy module (`src/modules/dummy/`, with a `DUMMY_ABI` override) is rebuilt at matching and mismatched ABI; `scripts/tests/test_abi_reject.py` pushes each variant, forces a clean reboot, asserts the new `chainloader_running()` liveness precondition, then reads both gates in both directions. The run is **bounded** (harness `time_budget` SIGALRM plus subprocess timeouts plus per-case `recover_probe`) so it cannot hang, and it now passes **6/6 on hardware** (wrong-ABI module + mutated pack each rejected newer and older; matching pair accepted; `chainloader_running` green each case). The gnwmanager multi-file push freeze that had blocked it was root-caused as an openocd stdout pipe-buffer deadlock and fixed (`push_batched.py` always drains via `GNWMANAGER_OPENOCD_DEBUG=1`, `73977ba`); the fork-vs-installed RAM app was never the variable. The host gate test also passes 8/8. Two reusable probe-test primitives landed: `chainloader_running()` (`scripts/common/harness.py`, standalone `scripts/tests/chainloader_running_test.py`), because a flash or push leaves the gnwmanager RAM flasher resident so liveness must be proven before trusting a probe read, and `time_budget()` for bounding probe ops. All three ABI stages (in-core gates, transient installer, rejection tests) are complete and hardware-verified. Plan: `.claude/plans/i-need-you-to-curried-kernighan.md`.
- **GOD-MODE BOOT-TO-OFW — restored (`restore-ofw-god-mode-boot`, hardware-verified 2026-06-01).** Two regressions from the fast-boot/fastcap reorg, same root pattern: the LEFT/RIGHT god-mode path (`main.c` `app_early_logic` §1.1) brings up the display + jumps into the OFW from *inside* `app_early_logic`, so it never reaches the DTCM-cleanup that `main()`/`input_init()` run on the menu path. (1) **"Preparing…" HardFault:** the OFW-flash progress bar → `gui_refresh()` → calls the fastcap hook (`FASTCAP_HOOK_ADDR` `0x2001FF04`); its reset-survivor junk (`0x30`) was cleared only *after* `app_early_logic()`, so god-mode `blx 0x30` → INVSTATE HardFault, frozen on "Preparing". Diagnosed live via `crash_log.py` (CFSR `0x20000` INVSTATE, faulting PC `0x30`, LR=`gui_refresh`); the OSPI-not-mapped hypothesis was *disproved* on device (OCTOSPI `CR`=`0x30000301`, FMODE already `0b11`). Fix: moved the `FASTCAP_HOOK_ADDR=0` clear to before `app_early_logic()`. (2) **OFW bounces straight back to the chainloader:** the bank-swap reset preserves DTCM; the patched OFW's `bootloader()` (`src/patch/main.c`) returns to the launcher on a stale `FORCE` in `SRAM_MAGIC` (`0x2001FFF8`), and `read_buttons()` ORs the remote-input shadow (`0x2001FFF4`) into the gamepad where LEFT+GAME reads as "return". The menu path clears both (`main` scrub + `input_init`); god-mode didn't. Fix: scrub `SRAM_MAGIC_ADDR` + `SRAM_REMOTE_INPUT_ADDR` before the god-mode OFW jump. Both halves hardware-verified (cold-boot LEFT flashes + boots the non-resident OFW and stays; the requested OFW no longer swaps back). Guard note: the `retrogo_return_test.py` proxy wasn't re-run (device disconnected post-test), but the new code is gated behind LEFT/RIGHT→OFW and never runs on a Retro-Go return — the stub/boot-magic checks and their order are untouched.
- **Regain lost flash (`regain-lost-space` branch, hardware-verified 2026-06-01): free 888 → 3252 B.** Three landed wins: (1) **FrogFS content access removed** (recognition + size kept; scanner reads the `FROG` header directly, viewer still labels it; no driver → file browser shows an empty list) — −848 B (`e8e708c`). (2) **i18n moved into a PIE language module** (`src/modules/language/` → `/modules/language.bin`): the former `ui/i18n.c` + `ui/font_ext.c` now live there; core = baked ASCII font + English `strings.c` + a thin `ui/i18n.c` shim + a `gui_glyph` render hook (`gui_set_ext_glyph`); module loads at boot when present (UTF-8 even in English), falls back to English-only when absent — −1352 B (render seam `72d1ebd`, move `fb29e43`). Verified: English boots without the module (Retro-Go round trip green); with it, the selector renders e.g. full **Greek**. (3) **partition-viewer + file-browser dedup** (`emit_group`, `tab_unmount`, `join_path`, dropped the dead FrogFS COPY-probe, `partition_driver_name` reuse) — −336 B (`70861e0`, `04f571c`). Reverted: `xz preset=9e` (regressed +164 B). Skipped (poor ROI / risk): the tree-walk merge (data-loss risk), the dcache/slot dedup (the duplicate copies are in modules / GC'd, not the core). Also fixed `retrogo_return_test.py` to sample Retro-Go in the reliable ~1 s handoff window (was false-failing at 3 s; `7ac4fc7`).
  - **Follow-ups (not done):** the OCR tests that read `g_current`/`g_langs` over SWD (`i18n_strings.py active_code`, used by `ocr_nav_test`/`theme_lang_test`/`i18n_switch`) will fail — those symbols moved into the module; repoint to OCR-only. Finish the remaining DESIGN.md FrogFS-as-browsable mentions (§7 feature list, the §11 backend table + browser-dispatch line); README "Multi-Language Menus" still implies in-core i18n.
- **i18n endonym glyphs missing from subsetted CJK fonts (fixed, hardware-verified 2026-06-01).** The first carousel capture showed the Japanese language name as `??語` — the subsetted `ja.fnt`/`ko.fnt` were built only from codepoints appearing in the *translation strings*, but the LANGUAGE selector also draws each language's **endonym** (`日本語`, `한국어`) which lives in the registry, not the packs. So `日`/`本`/`한`/`국` weren't in the font → `'?'`. Fixed by adding each language's endonym codepoints to its script's `.chars` set in `cook_lang.py build`; re-cooked + re-pushed the CJK fonts. All endonyms now render.
- **Probe contention from concurrent gnwmanager commands (process note).** Issuing a second probe command (`gnwmanager ls`) while a `gnwmanager push` was still running wedged OpenOCD (two openocd instances fighting for the ST-Link). Recovery: `pkill -f gnwmanager; pkill -f openocd`, then `trace.py reset-halt`. Lesson: serialize probe access — one foreground gnwmanager/openocd command at a time, never overlap a push with another probe op.
- **ASSET COOKER SCAN ROOT — clean-build break (fixed; pending on-device re-verify).** A `make clean && make` regenerated `src/chainloader/assets_gen.{c,h}` *without* the theme module's sprites, so `src/modules/theme/module_entry.c` failed to compile (`ASSET_* undeclared`: Mario coin/brick/yoshi/clouds, Zelda fairy). Root cause: `cook_assets.py` only scanned `src/chainloader/` for `ASSET_` usage, but the theme renderer is a PIE module under `src/modules/theme/` — its references were invisible, so the usage-based filter dropped those assets. The committed `assets_gen` was stale-but-complete (generated before the theme split), masking the break until a rebase/checkout freshened mtimes and re-ran the cooker. Fixed by broadening the cooker's `--src` default to `src/` (`scripts/build/cook_assets.py`); the regenerated `assets_gen.{c,h}` is byte-identical to the committed copy, so clean builds no longer dirty the tree. Was present identically on `main` and `fast-module-probe` (code-identical apart from four doc files). Surfaced while rebasing `fast_framebuffer_capture` onto `main`. Build is green; hardware re-verify of the themed menu still pending.
- **RETRO-GO RETURN-TO-MENU — stale stub jump fixed (hardware-verified 2026-06-01).** "Return to Main Menu" dropped to the chainloader menu instead of re-launching Retro-Go. A bank-1 Retro-Go bare-resets with only its `RESET` trace in `0x20000000`; the stub catches it and is meant to jump `RETROGO_BASE`, but jumped a hardcoded `0x08008000` that went stale when `693c6c7` raised the ceiling 32K→40K (`RETROGO_BASE`→`0x0800A000`). `0x08008000` is mid-image, so `board_is_valid_app` fails → `board_jump_to_app` no-ops → clean fall-through to the menu (the symptom — not a crash). Fixed the stub to jump `RETROGO_BASE` (the define, never a literal); also corrected `main.c` §2.1 so `CORE` re-launches (inverted in `a278f10`). Verified with `scripts/tests/retrogo_return_test.py`: `RESET` (stub) and `CORE` (main.c) both re-launch Retro-Go (PC in Retro-Go flash, chainloader header gone), and cold boot still reaches the menu. The GEMINI.md law had the direction backwards (→ menu) and is now corrected; DESIGN §5/§9 + `boot_magic.h` + `memory_map.h` aligned.
- **Boot/scan/theme speedup + menu rework — landed on `main` (hardware-verified).** Decoupled theme load from the full partition scan (front-loaded LittleFS probe at its inverted end-of-flash superblock) and overlapped the SD probe + whole scan with the LCD bring-up via a `gui_init` settle hook — boot-to-themed-menu ~469→415 ms with an SD card, ~1260→510 ms without (no-card detect now bails at the CMD0 gate, ~960→200 ms). Boot colour-flash eliminated (no placeholder theme). Also: per-OFW theme nibbles in `BKP3R`, consolidated `BOOT OFW: ‹console›` selector + top-level `SETTINGS` menu (Theme/Fast-Boot/Reset Defaults) with `ui_list` gray-out, and `REMOTE_INPUT` default-on. Detail + benchmarks: [docs/startup-module-probe.md](docs/startup-module-probe.md). Merged to `main` (`693c6c7` + `2b83952`).
- **OFW warm-switch power-off — RESOLVED (Mario + Zelda, hardware-verified 2026-05-31).** Switching into the stock OFW via a warm `NVIC_SystemReset` (our bank swap, or retro-go's reboot-to-stock) left the screen dark until a manual power press. Root cause: the OFW's pre-loop boot-mode selector gates display-init on `PWR_CPUCR.SBF` (set only on a genuine power-on, `0` after a warm reset), then the in-loop state-6 handler standbys because the power button isn't held. Fix: two byte-patches per firmware in `gnw_patch/{mario,zelda}.py` (NOP the SBF gate's `bpl`; state-6 standby `bpl`→`b`) — verified via `scripts/debug/probe_warmboot_display.py`: LTDCEN=1, PD4 high, OFW splash renders, no power press. Also renamed our patch entry symbol `chainloader`→`bootloader` to match unmodified gnwmanager (reset vector unchanged). Full write-up: [docs/ofw-poweroff-on-warm-switch.md](docs/ofw-poweroff-on-warm-switch.md). *Pending regression re-checks: cold boot, normal power-off, Left+Game escape.*
- **BOOT REGRESSION (128KB DICT):** Decompression hung because `props` in `stub_main.c` were hardcoded for 16KB. Fixed by synchronizing properties to `{0x5D, 0x00, 0x00, 0x02, 0x00}`.
- **BOOT REGRESSION (STUB):** Stub failed to boot because AXI-SRAM clocks weren't enabled. Fixed by moving the LZMA heap back to DTCM (80KB) and keeping the app decompression target in AXI-SRAM (Oneshot mode).
- **RETRO-GO RETURN:** Retro-Go warm resets were failing because the stub was overwriting game state in AXI-SRAM. Fixed by implementing a 4KB DTCM Safe Zone and stub-level direct-jumps for `CORE`/`RESET` magic words.
- **MARIO COLOR SHIFT:** Yoshi colors were garbage due to a 1-pixel offset in the NES sub-palette mapping (81 instead of 80). Corrected in `assets.c`.
- **FONT ORIENTATION:** Bitmaps for `<`, `>`, `/`, and `\` were inverted because lower bits are on the left. Corrected in `font_utils.c`.
- **STUB OVERFLOW (32 BYTES):** Resolved by removing HAL_PWR dependency from the stub and consolidating PD1/PD4 power-on to a single BSRR register write.
- **OFW THEME RELOAD:** Flashing a new OFW didn't trigger a theme switch. Fixed by ensuring `board_detect_console_type()` is called after the flash operation.
- **File Browser filename auto-scroll resolved.** Widened filename caches from 32 to 64 bytes and corrected `max_w` clipping boundaries in `ui_list_draw` to align with the actual screen width (320px) minus the starting offset.
- **LittleFS mount failure (-12) resolved.** Fixed by always declaring `prog_buffer` and setting `prog_size` to `LFS_CACHE_SIZE`.
- **"White Screen" after OSPI fix.** Consolidated power-on to `board_early_init`, matched the proven reset sequence.
- **THEME & UI REFINEMENTS:** Fixed `ui_current_theme` tracking, corrected Mario clouds animation trigger, removed glitchy walking Link, added animated fairy menu selector for Zelda, and fixed vertical spacing in context menu modals. Added standalone drivers Makefile with an `install` target to automate driver deployment.
- **DYNAMIC LFS RW DRIVER & UI POLISH:** Created dynamic LittleFS RW driver (`lfs_rw.bin` loaded at `0x240C0000` concurrently with `fatfs.bin` at `0x240A0000`). Changed tab formats in file browser to `1) SELECT FS >` / `< 2) SELECT FS`, removed copy/paste completion popup confirmations (only keep delete confirm), increased modal background blend opacity to 75%, and updated `ui_list_init` to skip default selection on dividers.
- **VFS COPY-PASTE AUTO-MOUNTING & UI FINISHING:** Implemented auto-mounting/driver-loading of the source partition in `perform_paste()` when copying files across tabs. Added `fs_is_rw` detection for the LittleFS partition label (`LITTLEFS RW` vs `LITTLEFS`). Created a dedicated press-any-button `ERROR` modal (`ui_show_error`). Fixed the Partition Viewer `-INTFLASH-` divider scrolling issue by initializing `scroll_y = 0`. Relocated `build_zelda` to `build/zelda` and copied the newly generated labeled Zelda tileset to `backup/`.
- **LFS WRITE FREEZE & FROGFS COPY ERROR:** Identified stack-use-after-return memory corruption in `lfs_vfs_open` across both host (`lfs_wrapper.c`) and dynamic (`module_entry.c`) LFS drivers due to temporary stack allocation of `struct lfs_file_config` (which the LittleFS file structures persist pointers to). Fixed by allocating the configs in static arrays. Added automatic unmounting of host LFS during dynamic driver loading (`vfs.c`) to prevent dirty mount states. Added compression rejection for FrogFS file access in `frogfs_reader.c` to fail gracefully on compressed files.
- **FROGFS FILENAME BUG & PARTITION SPACE PANE:** Fixed a critical bug in `frogfs_get_file_data` where `strcmp` was used on memory-mapped FrogFS filenames that are not null-terminated, causing file open failures; resolved by using segment-size-aware `memcmp`. Gated the context actions `COPY` on file accessibility so natively compressed files are safely ignored. Added `statfs` capability to the VFS and all drivers (host LFS, dynamic LFS RW, dynamic FAT, memory-mapped FrogFS), displaying Free and Used space in a new side pane on the partition select screen. Removed the `VIEW` action and prefix tab markers to save space, and refactored `format_size` using a DRY `format_unit` helper to format sizes with 1 decimal place (e.g. 1.5MB) when not exact multiples (resulting in 36 bytes of free space).
- **ZELDA THEME COLOR & GOLD BORDERS:** Adjusted Zelda background green to a lighter, tunic-inspired green (#1E6030) and brightened the gold accent (#E5B83B). Refactored window chrome, split panel separators, progress bars, and footer backgrounds to dynamically display gold accents/borders in the Zelda theme by introducing a global `gui_border_color` variable and `COLOR_BORDER` macro. This saved flash space, resulting in 44 bytes free (gained 8 bytes over the starting 36 bytes).
- **ZELDA SELECTION SPRITE:** Swapped the incorrect NES metasprite fairy for the correct Game & Watch clock fairy animation selection sprites (tile 190.0/190.1 left/right facing) as the item select indicator, centered at coordinate `x + 6`.
- **MENU ITEM VERTICAL CENTERING:** Dynamically centered menu items vertically in the Main and Tools menus when they contain fewer lines than the visible limits (using a mathematically exact centering offset calculation: `(h - count * 20 + 12) / 2`). Split pane lists (e.g. File Browser, Partition Viewer) are explicitly excluded from vertical centering, remaining top-aligned starting at `y + 5`. Reverted horizontal alignment back to left-aligned. Optimized list update logic, leaving 16 bytes free.
- **File Browser ".." Navigation:** Added ".." parent directory link in the file browser when inside subdirectories, blocking DELETE and COPY options on the parent link, and restoring directory highlight on back navigation. Verified build and flash.
- **FAT-RW module was never actually loading.** `mod_load` requires the GMOD PIE header, but `fatfs.bin` was still the old fixed-address (`0x240A0000`) module with no header, and the in-core RO FAT (registered under the same name) shadowed it via the early-return guard in `vfs_load_dynamic_driver`. So FAT was RO-only on-device. Fixed by converting `fatfs.bin` to a PIE/GMOD module and adding a `g_fat_rw_loaded` flag so the RW module registers over the RO one.
- **Copy-cancel never fired.** `update_progress_ui` doesn't call `input_update()`, and `input_just_pressed` only changes when it does — so the copy loop was polling stale button state. The long-operation pump now calls `input_update()` itself, so B/PWR genuinely abort copy/size/delete; the pre-flight size walk also pumps a ~30 fps "CALCULATING..." screen instead of freezing silently.
- **`statfs` overflowed on multi-GB cards.** Free/used were reported in bytes through a `uint32_t`, wrapping past 4 GB (an 8 GB card showed ~58 MB free). Changed the `statfs` contract to 512-byte SECTORS across every driver and formatted with `format_size_sectors`.
- **Folder copy/delete need recursion.** `lfs_remove` / `f_unlink` both refuse a non-empty directory, and a single `f_open`/copy can't recreate a tree — so deleting or copying a folder-with-contents silently failed. Added `copy_tree`/`delete_tree` using a re-scan-per-entry walk (`read_nth_entry`) that never holds a parent dir handle across recursion (the driver dir/file pools are only 2 deep) and re-reads index 0 while deleting (the listing shrinks underneath).
- **Filenames truncated at 47 chars** (the old fixed `file_names[16][48]` grid) lost the tail of long LFN names AND broke copy (the truncated name couldn't be `f_open`ed → "FAILED TO OPEN SRC FILE"). Replaced with a packed name pool holding full-length names for up to 512 files.
- **Orphaned `src/modules/filesystems/Makefile` footgun:** its `install` target rebuilt the modules with the *old* fixed-address recipe, which would clobber the root Makefile's PIE `.bin`s. Retired it and added a cohesive `push_modules` / `deploy` stage to the root build.



